export type ListType = {
    id: string,
    title: string,
    userId: string,
}

export type TasksType = {
    id: string,
    title: string, 
    description: string,
    done: boolean
}

export type UserType = {
    id: string,
    username: string
}
